<form method="post" action="{{ path('app_backoffice_mode_deleteModes', {'id': mode.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce mode de jeu ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode.id) }}">
        <button class="btn btn-sm btn-outline-danger mx-3 mt-2">Supprimer</button>
</form>